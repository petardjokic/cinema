<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="rs.ac.bg.fon.cinema.mapper.TicketMapper">

	<resultMap type="Ticket" id="TicketMap">
		<id column="id" property="id" />
		<result column="invoice_id" property="invoiceId" />
		<result column="display_id" property="displayId" />
		<result column="seat_id" property="seatId" />
	</resultMap>

	<resultMap type="TicketDto" id="TicketDtoMap">
		<id column="t.id" property="id" />
		<result column="t.display_id" property="displayId" />
		<result column="m.title" property="movieTitle" />
		<result column="h.name" property="hallName" />

	</resultMap>

	<!-- public long count(); -->
	<select id="count" resultType="Long">
		SELECT count(*) FROM tickets
	</select>


	<!-- public Ticket getById(Long id); -->
	<select id="getById" parameterType="long" resultMap="TicketMap">
		SELECT t.*
		FROM tickets t
		WHERE t.id = #{id}
	</select>

	<!-- public List<Ticket> getByDisplayId(Long displayId); -->
	<select id="getByDisplayId" parameterType="long"
		resultMap="TicketMap">
		SELECT t.*
		WHERE t.display_id = #{displayId}
	</select>

	<!-- public public List<TicketDto> getByInvoiceId(Long invoiceId); -->
	<select id="getByInvoiceId" parameterType="long"
		resultMap="TicketDtoMap">
		SELECT t.*, d.id, d.starts_at, m.title, h.name, hs.id,
		hs.seat_row, hs.seat_column, st.name
		FROM tickets t INNER JOIN displays
		d ON t.display_id = d.id INNER JOIN movies m ON d.movie_id = m.id
		INNER JOIN halls h ON d.hall_id = h.id INNER JOIN hall_seats hs ON
		hs.id = t.seat_id INNER JOIN seat_types st ON t.seat_type_id = st.id
		WHERE t.invoice_id = #{invoiceId}
	</select>

	<!-- public int insert(Ticket entity); -->
	<insert id="insert" parameterType="Ticket">
		<selectKey order="BEFORE" resultType="long" keyProperty="id">
			select sq_tickets.nextval from dual
		</selectKey>
		INSERT INTO tickets(id, invoice_id, display_id, seat_id)
		VALUES (#{id}, #{invoiceId} , #{displayId}, #{seatId})
	</insert>

	<!-- public int update(Ticket entity); -->
	<update id="update" parameterType="Ticket">
		UPDATE tickets
		SET
		invoice_id =
		#{invoiceId},
		display_id = #{displayId},
		seat_id = #{seatId}
		WHERE
		id =
		#{id}
	</update>

	<!-- public int deleteById(Long id); -->
	<delete id="deleteById" parameterType="long">
		DELETE
		FROM tickets
		WHERE
		id = #{id}
	</delete>


</mapper>